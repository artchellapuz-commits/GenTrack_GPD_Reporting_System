========================================
🔧 FIX ROLE-BASED UI - COMPLETE GUIDE
========================================

PROBLEM: Frontend not showing role-based UI changes (viewer1 still sees all menu items)

WHAT I FIXED:
✅ Fixed backend serializer syntax error (duplicate code in get_profile method)
✅ Added role badge CSS styles to AppLayout.vue
✅ Added menu divider and section title styles
✅ Verified role checking functions in auth.js
✅ Verified AppLayout.vue has v-if conditions for Upload and Water Nomination

========================================
STEP 1: RESTART BACKEND (CRITICAL!)
========================================

The backend serializer had a syntax error that's now fixed.
You MUST restart the backend server:

1. Stop the backend (Ctrl+C in backend terminal)
2. Start it again:
   cd backend
   python manage.py runserver

========================================
STEP 2: CLEAR FRONTEND CACHE
========================================

The frontend dev server is not picking up changes. Do this:

1. Stop the frontend (Ctrl+C in frontend terminal)

2. Clear the cache:
   cd frontend
   rmdir /s /q node_modules\.cache
   (or manually delete: frontend\node_modules\.cache folder)

3. Start frontend again:
   npm run serve

========================================
STEP 3: CLEAR BROWSER DATA
========================================

Your browser has cached the old code:

1. Open browser DevTools (F12)
2. Right-click the refresh button
3. Select "Empty Cache and Hard Reload"

OR

1. Press Ctrl+Shift+Delete
2. Clear "Cached images and files"
3. Clear for "All time"

========================================
STEP 4: RE-LOGIN
========================================

The old user data in localStorage doesn't have profile info:

1. Logout from the system
2. Login again with viewer1 / test123
3. The login will fetch fresh profile data with role

========================================
STEP 5: VERIFY IT WORKS
========================================

After re-login, viewer1 should:
✅ See role badge "Viewer" in sidebar
✅ NOT see "Upload Excel" menu item
✅ NOT see "Water Nomination" menu item
✅ Only see: Dashboard, View Reports, Generate Report

operator1 should:
✅ See role badge "Operator" in sidebar
✅ SEE "Upload Excel" menu item
✅ SEE "Water Nomination" menu item

manager1 should:
✅ See role badge "Manager" in sidebar
✅ SEE "Upload Excel" menu item
✅ SEE "Water Nomination" menu item

========================================
ALTERNATIVE: TEST BACKEND FIRST
========================================

Before restarting frontend, test if backend is working:

1. Run: TEST_LOGIN_API.bat
2. This will test login for viewer1, operator1, manager1
3. Check if response includes profile.role and permissions
4. If backend test passes, then restart frontend

========================================
QUICK COMMANDS
========================================

# Backend
cd backend
python manage.py runserver

# Frontend (in new terminal)
cd frontend
rmdir /s /q node_modules\.cache
npm run serve

# Test Backend API
TEST_LOGIN_API.bat

========================================
WHY THIS HAPPENS
========================================

1. Backend had syntax error → Fixed ✅
2. Frontend cache holds old compiled code → Clear cache
3. Browser cache holds old JavaScript → Hard reload
4. localStorage has old user data without profile → Re-login

All 4 must be addressed for changes to appear!

========================================
EXPECTED BEHAVIOR
========================================

VIEWER (viewer1):
- Can see: Dashboard, View Reports, Generate Report
- Cannot see: Upload Excel, Water Nomination, Admin Panel

OPERATOR (operator1):
- Can see: Dashboard, Upload Excel, View Reports, Generate Report, Water Nomination
- Cannot see: Admin Panel

MANAGER (manager1):
- Can see: Dashboard, Upload Excel, View Reports, Generate Report, Water Nomination
- Cannot see: Admin Panel (unless is_staff=True)

ADMIN:
- Can see: Everything including Admin Panel and Audit Logs

========================================
