[build]
  base = "npc-reporting-system/frontend"
  command = "npm run build"
  publish = "dist"

# Redirects to handle Vue Router's history mode
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
